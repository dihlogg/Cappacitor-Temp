import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'network-listen-app',
  webDir: 'dist/CapacitorCheckConnected',
};

export default config;
